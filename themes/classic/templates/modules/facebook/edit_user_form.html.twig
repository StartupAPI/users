<table>
	<tr>
		<td rowspan="2"><a href="{{me.link}}" target="_blank"><img src="http://graph.facebook.com/{{fb_id}}/picture" style="border: 0; max-width: 100px; max-height: 100px" title="{{me.name}}"></a></td>
		<td><a href="{{me.link}}" target="_blank">{{me.name}}</a></td>
	</tr>
	<tr>
		<td>
			<form style="margin: 0" action="{{action}}" method="POST" name="facebookusereditform">
				<input type="hidden" name="save" value="Save &gt;&gt;&gt;"/>
				<input class="btn btn-mini" type="submit" name="remove" value="remove" style="font-size: xx-small"/>
				<input type="hidden" name="CSRF_NONCE" value="{{ AUTH.CSRF_NONCE }}"/>
			</form>
		</td>
	</tr>
</table>